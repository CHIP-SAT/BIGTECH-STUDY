## 프로세스의 유형

- 포그라운드 프로세스: 사용자가 보는 공간에서 사용자와 상호작용하여 실행되는 프로세스
- 백그라운드 프로세스: 사용자가 보지 못하는 곳에서 실행되는 프로세스
- 데몬: 사용자와 별다른 상호작용 없이 주어진 작업만 수행하는 특별한 백그라운드 프로세스

---

## **하나의 프로세스를 구성하는 메모리 내의 정보**

![image.png](attachment:5abefdb4-b79f-4cd3-ba21-df37144af6ca:image.png)

- 커널 영역
    - 프로세스 제어 블록(PCB)
- 사용자 영역
    - 코드 영역(텍스트 영역)
        - 실행 가능한 명령어가 저장되는 공간
        - 읽기 전용 공간(read-only)
    - 데이터 영역
        - 프로그램이 실행되는 동안 유지할 데이터가 저장되는 공간
        - 정적 변수, 전역 변수
        - BSS 영역
            - 데이터 영역과 차이점 →초기화 여부
            - 프로그램을 사용하는 동안 유지할 데이터 중 초깃값이 있는 정적 변수나 전역 변수와 같이 초깃값이 없는 데이터
    - 힙 영역
        - 프로그램을 만드는 사용자가 직접 할당 가능한 저장 공간
        - 힙 영역에 메모리 공간을 할당했다면 언젠가는 해당 공간을 반환해야함.
            - 메모리 누수: 할당 받은 메모리 공간을 반환하지 않아 메모리를 낭비하는 문제
    - 스택 영역
        - 일시적으로 사용할 값들이 저장되는 공간
        - 함수의 실행이 끝나면 사라지는 매개변수, 지역 변수, 함수 복귀 주소 등이 저장됨.
        - 스택 트레이스 형태의 함수 호출 정보가 저장될 수 있음.
            - 스택 트레이스: 특정 시점에 스택 영역에 저장된 함수 호출 정보
            - 문제의 발생 지점 추적 가능 → 디버깅에 유용

---

## 프로세스 제어 블록(PCB)

> 프로세스를 식별할 수 있는 커널 영역 내의 정보
> 
- 프로세스와 관련된 다양한 정보를 내포하는 구조체의 일종
- 새로운 프로세스가 메모리에 적재 됐을 때(프로세스 생성) **커널 영역**에 만들어지고, 프로세스의 실행이 끝나면 폐기됨.
- 여러 PCB들은 커널 내에 프로세스 테이블의 형태로 관리됨
    - 프로세스 테이블: 실행 중인 PCB의 모음
    
    ![image.png](attachment:c67aecd0-0dae-4112-bdf1-2382330b68e2:image.png)
    
- 좀비 프로세스: 프로세스가 비정상 종료되어 사용한 자원이 회수되었음에도 프로세스 테이블에 종료된 프로세스의 PCB가 남아있는 경우

**PCB에 담기는 정보**

- 프로세스 ID(PID)
- 프로세스가 실행 과정에서 사용한 레지스터 값
- 프로세스가 현재 어떤 상태인지를 나타내는 프로세스 상태
- 프로세스가 언제, 어떤 순서로 CPU를 할당받을지 나타내는 CPU 스케줄링
- 프로세스의 메모리상 적재 위치를 담은 메모리 관련 정보
- 프로세스가 사용한 파일 및 입출력장치 관련 정보

---

## 문맥 교환

> 기존 프로세스의 문맥을 PCB에 백업하고, PCB에서 문맥을 복구하여 새로운 프로세스를 실행하는 것
> 
- “프로세스가 실행된다” == “운영체제에 의해 CPU의 자원을 할당 받았다”
- 프로세스의 CPU 사용 시간은 **타이머 인터럽트**에 의해 제한됨.
    - 타이머 인터럽트(타임아웃 인터럽트): 시간이 끝났음을 알리는 인터럽트
- 프로세스 실행 중 타이머 인터럽트가 발생하면, 해당 프로세스는 카운터를 비롯한 각종 레지스터 값과 메모리 정보, 실행을 위해 열었던 파일, 사용한 입출력 장치 등 지금까지의 중간 정보를 **백업**해야함.

**문맥**

- 프로세스의 수행을 재개하기 위해 기억해야 할 정보
- 프로세스의 문맥은 해당 프로세스의 PCB에 명시됨.
- 타이머 인터럽트가 발생하면 운영체제는 해당 프로세스의 PCB에 문맥을 백업하고, 다음에 실행할 프로세스의 문맥을 복구함.

![image.png](attachment:9bcfac3f-a583-41e6-8ac5-c36d611af1d8:image.png)

**프로세스 간에 잦은 문맥 교환의 문제점**

- 캐시 미스 발생 가능성 상승 → 메모리로부터 실행할 프로세스의 내용을 가져오는 작업 상승 ⇒ 큰 오버헤드

---

## 프로세스의 상태

- 운영체제는 PCB를 통해 프로세스의 상태를 인식하고 관리함.

![image.png](attachment:d29ac50f-017f-492f-9fcd-50afef64a2bb:image.png)

- 생성 상태(new)
    - 프로세스를 생성 중인 상태
    - 메모리에 적재되어 PCB를 할당받은 상태
- 준비 상태(ready)
    - 당장 CPU를 할당받아 실행할 수 있지만, 아직 자신의 차례가 아니기 때문에 기다리고 있는 상태
    - 디스패치: 준비 상태인 프로세스가 실행 상태로 전환되는 것
- 실행 상태(running)
    - CPU를 할당받아 실행 중인 상태
    - 타이머 인터럽트 발생 → 준비 상태로 변경
    - 입출력장치 사용 → 대기 상태로 변경
- 대기 상태(blocked)
    - 프로세스가 입출력 작업을 요청하거나 바로 확보할 수 없는 자원을 요청하는 등 곧장 실행이 불가능한 조건에 놓이는 경우
- 종료 상태(terminated)
    - 프로세스가 종료된 상태

---

## 블로킹 입출력과 논블로킹 입출력

- 블로킹 입출력: 입출력 작업 요청이 들어와 대기 상태로 접어들고, 입출력 작업이 완료되면 준비 상태가 되어 실행을 재개하는 입출력 방식
    
    ![image.png](attachment:b1485547-f56a-44c5-abef-4eaf0e336620:image.png)
    
- 논블로킹 입출력: 입출력장치에게 입출력 작업을 맡긴 뒤, 곧바로 이어질 명령어를 실행할 수 있는 입출력 방식
    
    ![image.png](attachment:00686593-4026-4714-9961-e8d37827a620:image.png)
    

---

## 멀티프로세스

> 동시에 여러 프로세스가 실행되는 것
> 

![image.png](attachment:7d7d1f60-15e3-4037-86a9-33fe5e19257a:image.png)

- 각 프로세스들은 기본적으로 자원을 공유하지 않고, **독립적**으로 실행됨.
- 각각의 PID 값이 다름.
- 프로세스별로 파일과 입출력장치 등의 자원이 독립적으로 할당됨. → 다른 프로세스에 영향을 끼치지 X

---

## 멀티스레드

> 한 프로세스를 동시에 실행하는 여러 스레드
> 

![image.png](attachment:80811463-cc71-496c-883b-21242f6e2013:image.png)

- 스레드의 구성
    - 스레드 ID, 프로그램 카운터, 레지스터 값, 스택 등
- 스레드마다 각각의 프로그램 카운터 값과 스택을 가지고 있기 때문에, 스레드마다 다음에 실행할 주소를 가질 수 있고, 연산 과정의 임시 저장 값을 가질 수 있음.

---

## 멀티프로세스와 멀티스레드의 차이점

> 자원의 공유 여부
> 
- 멀티프로세스
    - 자원을 공유하지 X
    - 한 프로세스에 문제가 생겨도 다른 프로세스에 영향 X
- 멀티스레드
    - 프로세스의 자원을 공유 O
    - 한 스레드에 생긴 문제가 프로세스 전체의 문제가 될 수 O

---

## 프로세스 간 통신(IPC)

> 프로세스 간에도 자원을 공유하고 데이터를 주고받을 수 있는 방법
> 
- 프로세스 간 통신이 이루어지는 방식
    - 공유 메모리
    - 메시지 전달

**공유 메모리**

![image.png](attachment:97fcf243-3074-4fcd-ac63-ee27913d3b06:image.png)

- 프로세스 간에 공유하는 메모리 영역을 도대로 데이터를 주고받는 통신 방식
- 활용 방식
    - 프로세스가 공유하는 메모리 영역을 확보하는 **시스템 콜**을 기반으로 수행
    - 프로세스가 공유하는 **변수**나 **파일**을 활용
- 통신을 주고받는 각 프로세스가 마치 자신의 메모리 영역을 읽고 쓰는 것처럼 통신
- 프로세스가 데이터를 주고받는 과정에 커널의 개입이 거의 X
- 별도의 독립적인 영역인 힙과 스택 사이의 라이브러리/매핑 영역에 위치
- 장점
    - 메시지 전달 방식보다 통신 속도가 빠름.
- 단점
    - 레이스 컨디션이 발생할 수 있음.
        - 레이스 컨디션: 각각의 프로세스가 서로의 공유 메모리 영역을 동시에 읽고 쓰는 경우, 데이터의 일관성이 훼손되는 것

**메시지 전달**

![image.png](attachment:69ce8191-bfb5-4758-be66-edf137563f61:image.png)

- 프로세스 간에 주고받을 데이터가 커널을 거쳐 송수신되는 통신 방식
- 메시지를 보내는 시스템 콜: `send()`
- 메시지를 받는 시스템 콜: `recv()`
    
    ⇒ 각 프로세스는 두 시스템 콜을 호출하며 메시지를 송수신
    
- 공유 메모리 기반 IPC보다 커널의 도움을 적극적으로 받을 수 있으므로 레이스 컨디션, 동기화 등의 문제를 고려하는 일이 상대적으로 적음.
- 직접적으로 메시지를 주고받지 않고, **비동기적**으로 원하는 동작 수행 가능
- 코어 덤프: 비정상적으로 종료하는 경우 생성되는 파일로, 프로그램이 특정 시점에 작업하던 메모리 상태가 기록됨.
- 단점
    - 공유 메모리 기반 IPC보다 통신 속도가 느림.

**메시지 전달 기반 IPC를 위한 대표적인 수단**

- 파이프: 단방향 프로세스 간의 통신 도구
    - 익명 파이프: 양방향 통신을 지원하지 않고, 부모 프로세스와 자식 프로세스 간에만 통신이 가능
    - 지명 파이프(FIFO, 네임드 파이프): 양방향 통신을 지원하고, 임의의 프로세스 간에도 사용 가능
- 시그널: 프로세스에게 특정 이벤트가 발생했음을 알리는 비동기적인 신호
    - 프로세스는 시그널이 발생하면 하던 일을 중단하고, 시그널 처리를 위한 시그널 핸들러를 실행한 뒤 시그널 핸들러가 종료되면 중단했던 작업을 재개함.
- 소켓
- 원격 프로시저 호출(RPC): 다른 프로세스의 원격 코드 실행
    - 로컬 프로시저 호출: 한 프로세스 내의 특정 코드 실행
    - 프로그래밍 언어나 플랫폼과 무관하게 성능 저하 최소화, 메시지 송수진 가능 → 대규모 트래픽 처리 환경에서 사용되는 경우가 많음.
    - HOW? → 일반적인 REST API는 인간이 읽기 쉬운 JSON 형식을 쓰지만, RPC는 이진 데이터를 압축하여 사용하기 때문에 데이터 크기가 획기적으로 줄어들어 네트워크 대역폭을 적게 차지하고, 데이터를 읽고 쓰는 속도도 매우 빨라짐.
